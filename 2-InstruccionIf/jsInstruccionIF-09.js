var max = 10;
var min = 0;

function mostrar() {
	//Genero el número RANDOM entre 1 y 10 
	var numero_entero = Math.round(Math.random() * (max - min) + min);
	alert(numero_entero);
}//FIN DE LA FUNCIÓN

